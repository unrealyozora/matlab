v_riga= [1,2,3] %vettore riga
v_colonna=[1;2;3] % vettore colonna
r= v' %vettore v trasposto
t = 0:1/2:sqrt(2) % vettore che parte da 0 , ogni elemento successivo e' il multiplo di 1/2, si ferma al multiplo piu' piccolo di sqrt(2)
x= linspace(0,1,5) % vettore che va da 0 a 1 diviso in 5 parti uguali

A=[1,2,3;4,5,6] % matrici
zeros(m,n) %matrice mxn di zeri
ones(m,n) % matrice mxn di uni
eye(n) % matrice identita' 

F=[2,3,4;5,6,7]
